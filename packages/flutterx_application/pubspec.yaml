name: flutterx_application
description: "Use cases orchestrating the engines and ports; the API the CLI and future daemon consume (docs/06 §4)."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  flutterx_domain: ^0.1.0
  flutterx_intelligence: ^0.1.0
  crypto: ^3.0.6
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
