/// Pure implementations of the SDK Intelligence engines: scanner, solver, rules, recommendation, dependency intelligence, upgrade advisor, repair planner (docs/03, docs/06 §3).
///
/// This barrel is the package's only public entry point; everything under
/// `src/` is private (docs/06 §1). Exports are added as types land.
library;
