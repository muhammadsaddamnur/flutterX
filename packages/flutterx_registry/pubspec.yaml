name: flutterx_registry
description: "Releases-index and pub.dev metadata clients with snapshot caching (docs/03 §1, docs/06 §7)."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  flutterx_domain: ^0.1.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
