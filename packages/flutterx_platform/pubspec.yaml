name: flutterx_platform
description: "OS abstraction: paths, links, process execution, shims (docs/06 §8)."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  flutterx_domain: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
